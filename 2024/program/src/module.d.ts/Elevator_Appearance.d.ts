declare interface IElevator_Appearance {
  db_id: string
  db_id_appearName: string // 现象名称
  db_appearDesc: string // 现象描述
  db_note: string // 备注
  db_solution: string // 解决方案
  db_workItemIds: string // 现象工作项id集合，逗号拼接
}
