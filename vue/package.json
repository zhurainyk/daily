{
  "name": "vue",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"cd test && cd app && npm run serve",
    "parallel":"cd test && cd app && npm-run-all --parallel serve build",
    "serial":"cd test && cd app && npm-run-all --serial build serve",
    "start-all":"cd test && cd app && npm-run-all --parallel start:*",
    "install-t":"npm install {1}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
